function [valid,suggested] = RN_checkExPath(exPath)
    suggested = {'Fix Filenames','Create Trodes Comments','Export Spikes','Export LFP','Export Time','Export DIO','Generate Matclusts'};
    valid = 0;
    valid = 1;
end
